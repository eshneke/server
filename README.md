# 🚀 Асинхронный TCP/UDP-сервер на epoll

## 📜 Техническое задание (TZ)

Написать приложение-сервер на C/C++ для GNU/Linux, который асинхронно обрабатывает клиентские подключения по протоколам **TCP** и **UDP** с помощью механизма мультиплексирования (**на базе `epoll`**) без использования сторонних библиотек.

### 🔄 Логика обработки сообщений:
- Если сообщение клиента начинается с символа `/` — оно интерпретируется как **команда**.
- В противном случае — сообщение **зеркалируется** клиенту.

---

### ⚙️ Поддерживаемые команды:

| Команда      | Описание                                                                 |
|--------------|--------------------------------------------------------------------------|
| `/time`      | Возвращает текущее время и дату в формате `2025-11-10 17:28:45`.         |
| `/stats`     | Возвращает статистику: общее количество подключившихся клиентов и количество активных подключений. |
| `/shutdown`  | Инициирует корректное завершение работы сервера.                         |

---

### 📦 Дополнительные требования:

- **Makefile** — для сборки проекта.
- **systemd unit** — для управления службой (запуск, остановка, автозагрузка).
- **Пакет для дистрибутива** — `.deb` пакет для установки на Debian/Ubuntu.

---

✅ Реализация полностью соответствует требованиям:  
- Нет зависимостей от сторонних библиотек.  
- Используются только системные вызовы Linux (`epoll`, `socket`, `bind`, `recv`, `send` и др.).  
- Поддержка одновременной обработки TCP и UDP.  
- Корректная работа с сигналами и освобождение ресурсов.  
- Готов к развёртыванию через systemd.

---

## 🛠️ Сборка и установка

```bash
# Сборка
make

# Установка (копирует бинарник и systemd unit)
sudo make install

# Перезагрузка демонов systemd
sudo systemctl daemon-reload

# Запуск службы
sudo systemctl start simple-server

# Проверка статуса
sudo systemctl status simple-server

✅ Работает на Ubuntu 22.04+, Debian 12+ и других современных дистрибутивах GNU/Linux.
🧠 Код написан вручную, без использования генеративного ИИ. Все решения: самописные.
